# =========================
# ECLPS hyprlock palette
# =========================

# Core text
$lock_text     = rgba(235, 235, 235, 1.0)   # ash-white
$lock_text_dim = rgba(235, 235, 235, 0.72)  # softened
$lock_textalt = rgba(80, 65, 80, 0.92)  # ash

# Glass + input field
$lock_glass = rgba(200, 200, 200, 0.15)        # smoky glass pane
$lock_field = rgba(10, 10, 12, 0.45)        # slightly darker for the input
$lock_imageback = rgba(0,0,0,0.92)

# Accent (gold rim) + glow
$lock_accent = rgba(210, 180, 90, 0.95)     # thin gold accent
$lock_glow   = rgba(242, 211, 138, 0.25)     # soft halo glow
$bar_fill = rgba(210, 180, 90, 1) 

# Optional extra highlight (rare violet / moonlight) if you want later:
# $lock_accent = rgba(175, 197, 218, 0.92)  # moonlight-blue variant
# $lock_glow   = rgba(175, 197, 218, 0.24)

# =========================
# Backwards-compat mapping
# (so any configs referencing these still look right)
# =========================
$color        = $lock_glass                # card body
$inner_color  = rgba(235, 235, 235, 0.10)  # subtle inner sheen, not bright fill
$outer_color  = $lock_accent               # border
$font_color   = $lock_text                 # labels
$check_color  = rgba(175, 197, 218, 0.95)  # "success" tint = moonlight highlight

# =========================
# Paths / font
# =========================
$lock_bg   = /home/kazuki/.config/omarchy/themes/eclps/backgrounds/Eclipse-grid.jpg
$lock_logo = /home/kazuki/.config/omarchy/themes/eclps/ECLPS_OS_Logo.png
$lock_font = Zen Maru Gothic, Zen Maru Gothic Black

